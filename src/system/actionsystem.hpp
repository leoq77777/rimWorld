#include "world/world.h"

class ActionSystem{
    public:
        ActionSystem(World& world);
        void collect(Entity entity) {}
        void move(Entity entity) {}
        void 
        void cut() {
            
        }
    private:
        World& world;
};